#include <bits/stdc++.h>
using namespace std;

void solve()
{
    // input
    int n;
    cin >> n;
    string s;
    cin >> s;

    // main 
    cout << s[n -1] << endl;    

    // output

}

int main()
{
    ios_base::sync_with_stdio(false), cin.tie(nullptr);

    int t;
    cin >> t;
    while (t --) {
        solve();
        // cout << (solve() ? "Yes" : "No");
        // cout << solve() << endl;
    }
    return 0;
}